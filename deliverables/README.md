# 交付清单 · GuideSphere 引导球

本文件只说明**这一包交付了什么、验证到什么程度**。

- 安装 / 操作 / 参数 / 常见问题 → [`../README.md`](../README.md)
- 架构 / 坐标系 / 算法 / 踩坑 → [`../docs/IMPLEMENTATION.md`](../docs/IMPLEMENTATION.md)

---

## 一、交付物

| 文件 | 说明 |
| --- | --- |
| `GuideSphere.apk` | 可直接安装的 release 包（含 x86 / x86_64 / arm64-v8a / armeabi-v7a） |
| `guidesphere_demo.mp4` | 演示录屏，32 秒 / 720×1600 / 2.2 MB。绕物体水平转一圈，8 个面依次整面点亮，`25% → 100%`，收尾弹出「360° 无死角覆盖完成」横幅 |
| `guidesphere_gray_sphere.png` | 运行截图（模拟器 720×1600）：灰色球壳、8 面整面点阵、已拍 `3/8` 面、目标面整面琥珀高亮、中央文字 `已标记 3/8 个面` |
| `guidesphere_real_device.png` | 真机截图（一加 PKX110，1080×2344，球停靠底部 / 覆盖 100%）。**为上一版视觉**（蓝色球壳 + 中央 8 段环），性能数据仍有效；最新视觉以演示视频与上图为准 |

### 安装

```bash
adb install -r GuideSphere.apk
```

首次启动需授予相机权限（拒绝也可运行，会退化为「引导球 + 渐变底」）。
本包为**演示签名**，正式发布请用自有密钥重新签名（做法见根 README 第三节）。

---

## 二、演示视频里能看到什么

> 时间轴对应 `guidesphere_demo.mp4`（约 32 s：绕物体水平转一圈，8 个面依次整面点亮）。

1. **00:00 – 00:05 开场。** 相机实时画面铺满全屏（模拟器内置合成场景），画面底部停靠一个
   **灰色底色**的半透明引导球（菲涅尔边缘光 + 经纬网格线），球面按 **8 个方位扇区整面铺开点阵**
   （每面 3 列 × 7 行 = 21 点，共 168 点）。**镜头此刻正对的那个面已经被记为「已拍」**
   （`25%`，已拍 `2/8` 个面）—— 这是「一次拍摄 = 一个面」的直接体现：对准即拍下。
   屏幕中央是文字 **「绕物体旋转一周」**，下方小字 `已标记 2/8 个面`；
   左上角是覆盖度卡，右上角是 `60 FPS 0.1ms`、姿态来源、「相机画面 ✓」与「自动巡航」按钮。
2. **00:05 – 00:26 绕物体水平转一圈。** 镜头每转到一个新的面并稳定停留 `≈0.22 s`
   （自动采样），**该面的 21 个点整面一起点亮**（青绿点阵），覆盖度按 `+1/8` 跳。
   **目标面**（下一个该拍的面）以**琥珀色整面点簇**高亮，球外圈有一枚**琥珀色方向箭头**
   指向镜头该往哪边偏。覆盖度 `25% → 50% → 75%（6/8）→ 100%`。
   中途左上角弹出「已捕获 +N」的浮动提示。
3. **00:26 – 00:32 收尾。** 覆盖度到达 `100% / 已拍 8/8 个面`，覆盖度环变绿，
   顶部弹出「360° 无死角覆盖完成 / 可以提交给重建算法了」，底部状态变为「全部覆盖 ✓」。
   全程 `60 FPS`。

> 视频在**模拟器**上录制（相机画面是模拟器内置的合成场景），但**姿态解算、整面覆盖判定、
> 覆盖率统计、60 FPS 渲染全部是真实的运行时行为**，不是播放动画。
> 姿态来自模拟器控制台**注入的真实加速度计 / 磁力计读数**，走的是 App 真实的传感器代码路径。
> 之所以不在真机录，是因为一加 PKX110 的 ColorOS 封禁了 `adb shell screenrecord` 写文件
> （原因与排查过程见[实现文档 §八](../docs/IMPLEMENTATION.md)）。

---

## 三、验证记录

### 3.1 模拟器（720×1600，`-gpu host`）

| 项目 | 结果 |
| --- | --- |
| 帧率 / 单帧 | **60 FPS**，`drawMs` 0.07 ~ 0.13 ms |
| 球体停靠 | `rPx=122`、`cy=1088` = `1600 × 0.68`，与真机同比例 |
| 绕物体环绕 | 单圈水平环绕一次性到达 **100%（168/168，8/8 个面，零补扫）**，约 24 s |
| 面捕获 | 每正对一个新面 `lit` 精确 **+21**，8 次到 168：`21 → 42 → 63 → 84 → 105 → 126 → 147 → 168` |
| 优化后回归 | 同上逐面步进复测 8 步全对，`faces=8/8`，`drawMs` 0.07 ~ 0.11 ms，无回退 |

### 3.2 真机（一加 PKX110 / Android 16 / SDK 36 / arm64-v8a / 1080×2344）

> 下表为**上一版视觉**（蓝色球壳 + 中央 8 段环 + 140 点）的实测记录。
> 渲染管线与绘制量级相同，性能与生命周期结论对当前版本仍成立；
> 新版视觉（灰球 + 168 点阵 + 文字引导）的**真机复测待设备接入后进行**。

| 项目 | 结果 |
| --- | --- |
| 冷启动 | 约 56 ms 进入引导页 |
| 帧率 / 单帧 | 稳定 **60 FPS**，0.3 ~ 0.8 ms |
| 崩溃 | 无 |
| 相机预览 | CameraX 后置实时画面作为背景，正常 |
| 姿态来源 | 静置 20 s 采样 60 次，**60/60 全部为「陀螺仪姿态」**，零翻转 |
| 传感器事件 | 旋转矢量实测约 89 Hz（`dumpsys sensorservice` 计数 1332 events / 15 s） |
| 球体停靠 | `rPx=184`、`cy=1593` = `2344 × 0.68`，上方画面完整留给被摄物体 |
| 生命周期 | HOME 回前台、息屏/亮屏、连续重复启动，渲染线程数恒为 1，无泄漏 |

### 3.3 已知未验证项

- **新版视觉的真机复测**：灰球 + 168 点阵 + 文字引导尚未在真机上跑过（设备未接入）。
- **非 ColorOS 机型的录屏**：演示视频仅在模拟器录制，未在其他真机上验证录屏链路。
- **极端俯仰**：一次拍摄只判定方位面，上下两极的覆盖不进判定；若需要「上/下也各算一个面」，
  需要按根 README 第八节的说明扩展扇区划分。

---

## 四、复现方式

```bash
# 1. 启动模拟器（无显示环境要加 -no-window，否则会弹阻塞式对话框；-gpu host 是 60fps 前提）
emulator -avd GuideSphere -gpu host -no-boot-anim

# 2. 安装并启动
adb install -r GuideSphere.apk
adb shell am start -n com.remy.guidesphere/.MainActivity

# 3. 注入真实的加速度计/磁力计读数，绕物体水平转一圈
python ../.tools/drive.py sweep 1 3 0 1 "8"

# 4. 看覆盖度遥测
adb logcat -s GuideSphereStats:I
# I GuideSphereStats: cov=100.0% lit=168/168 faces=8/8 cam=6 target=0 fps=60 drawMs=0.07 rPx=122 cy=1088 next=(0.00,0.00) src=陀螺仪姿态

# 一键：安装 → 启动 → 环绕 → 闭环补扫 → 录屏 → 拉回 mp4
python ../.tools/record_demo.py <apk路径> <输出mp4路径>
```

`.tools/` 下的脚本依赖本机 Python 与 adb，**不参与 App 构建**。
