# ---------------------------------------------------------------- Android / Gradle
build/
.gradle/
.cxx/
captures/
*.iml
.idea/
local.properties

# ---------------------------------------------------------------- 签名材料
# 绝不把私钥与口令提交到版本库。需要出 release 签名包时，
# 在 GuideSphere/ 下本地创建 keystore.properties（格式见根 README 第三节）。
keystore.properties
*.jks
*.keystore

# ---------------------------------------------------------------- 本机工具与中间产物
# .tools/ 下只保留脚本；注入日志、截图、录屏、SDK 压缩包等都是本机临时产物
.tools/*
!.tools/*.py
!.tools/README.md

# ---------------------------------------------------------------- 工作区临时文件
.workbuddy/
_env.txt
_ls.txt
# 模拟器启动日志（排查启动失败时用，不入库）
emu.log
*.log

# ---------------------------------------------------------------- 系统
.DS_Store
Thumbs.db
desktop.ini
